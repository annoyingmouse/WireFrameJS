const maxHeightPlatform = 124

import { platforms } from './Platforms'
import { platformNames } from './PlatformNames'
import { platformLines } from './PlatformLines'


export class AllPlatforms {
  constructor() {
    this.platformActors = platforms.map((element, index) => {
      
    })
  }
}